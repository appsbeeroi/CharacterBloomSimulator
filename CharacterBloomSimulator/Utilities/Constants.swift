import SwiftUI

struct Constants {
    static let maxMood = 100
    static let maxSatiety = 100
    static let maxPurity = 100
    static let maxGrowthStage = 4

    struct Colors {
        static let primaryOrange = Color.orange
        static let backgroundGradient = [Color.yellow.opacity(0.1), Color.green.opacity(0.1)]
        static let gameGradient = [Color.pink.opacity(0.2), Color.orange.opacity(0.2)]
    }
}
